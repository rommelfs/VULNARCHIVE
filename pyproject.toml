[project]
name = "fd-sightings"
version = "0.2.0"
description = "VULNARCHIVE GNA 1988 mailing-list archive and GCVE publication pipeline"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
fd-sightings = "fd_sightings.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
